# Current ReadMark v3 Status

Last updated: 2026-08-16.

## Current State

- ReadMark `0.6.0-beta.3` is unfinished and unreleased.
- The protocol-v3 pre-render semantic architecture is accepted through Q383 in `ADR/2026-07-19-pre-render-semantic-network.md`.
- Protocol-v3 semantic implementation is integrated on feature branches. The accepted protocol and Prompt contracts remain frozen through Q493; structural contradiction Q505 is resolved in the structural ADR.
- The plugin and Runner now contain byte-identical closed protocol-v3 Schema/type authorities, independent validators, deterministic scoring implementations, and public synthetic cross-repository vectors.
- The Runner now contains the strict two-message Prompt manifest/runtime, exact-render fixtures, bounded recovery primitives, sanitized PromptTrace, provider identity binding, and selective checkpoint reuse/invalidation.
- Runner shared semantic preparation is implemented and independently accepted: Term materialization and nonliteral audit, explicit deterministic anomaly input, stratified Source Disposition review/adjudication, strict request caps, Generic Filter discard boundaries, and Attribute proposal/critic/repair.
- The Runner completes the fixed v3 semantic sequence through deterministic Evidence Clusters, three independent Runs, revision assembly, scoring/selection, atomic publication, truthful closed events, and completion-last signaling. Active CLI commands are limited to `configure/doctor/probe/run/status/report`.
- The Plugin builds the v3 Source Snapshot, exports and resumes exact task bundles, consumes closed Runner events, imports only a fully validated `complete.json` through two freshness gates, retains three immutable imports, independently validates/scorers selection, and activates the Semantic Report view. The old Book World, renderer, art, manual v2 import, unlock, and activity-score implementation/tests have been removed from the active source tree after the replacement fake-provider E2E passed.
- Versioned checkpoints now exist for documentation, protocol/scoring, Prompt runtime, and shared semantic preparation. The plugin documentation branch is pushed; the Runner feature branch has local commits but no configured remote.
- A public synthetic cross-repository fake-provider E2E now covers Plugin export, compiled Runner three-candidate completion, Plugin double-freshness import, and Semantic Report construction. No fresh private MiniMax-M3 v3 task has been run.
- No v3 result has received manual Obsidian acceptance.
- Existing local implementation changes in both repositories predate this documentation normalization and must not be reverted.

## Next Authorized Objective

Finish the two-repository fake/schema/golden verification record and pause. A real MiniMax-M3 task and manual Obsidian acceptance require separate explicit approval. Do not begin 2.5D work.

Maintain `EVALS.md` and this status after every reviewed phase. Questions that code or existing documents can answer must be resolved mechanically. Q001-Q493 remain frozen unless a mechanically provable contradiction is found.

## Hard Stops

- Do not inspect, resume, migrate, or import the obsolete protocol-v2 task as v3 truth.
- Do not run a real provider task during the Prompt Grill.
- Do not call SenseNova, generate images, restore art routes, or begin 2.5D rendering.
- Do not invent Prompt or semantic behavior outside Q001-Q493.
- Frequent scoped commits and pushes are authorized as recorded in `DEVELOPMENT.md`; tag, release, clean, reset, restore, and historical-data deletion remain prohibited.
- Do not expose credentials, endpoints, private task paths, checkpoints, source snapshots, book content, model output, or plugin runtime data.

## Completion Path After Prompt Design

Protocol v3 implementation and fake-provider/schema/golden-vector verification are the current completed scope. The next separately authorized path is one fresh private MiniMax-M3 v3 task with zero image calls, followed by independent Plugin validation/import and manual Semantic Report acceptance in Obsidian. Stop before 2.5D.
